PORT=5000
FRONTEND_URL=http://localhost:5173
# For nodemailer (SMTP). Use Gmail SMTP, SendGrid SMTP or any SMTP provider.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=yourpassword

# Or for SendGrid API (optional)
SENDGRID_API_KEY=

# Admin basic-auth (optional) to view submissions
ADMIN_USER=admin
ADMIN_PASS=changeme
